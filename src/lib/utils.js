export function has(map, el) {
  return map.has(el);
}

export function get(map, el) {
  return map.get(el);
}

export function set(map, el, v) {
  return map.set(el, v);
}

export function del(map, el) {
  return map.delete(el);
}
